name: 'Infra - Dev'
on:
  push:
    branches:
    - dev
    path:
    - 'terraform/*'
    - '.github/workflows/ci-dev.yml'

  pull_request:
    branches:
    - dev
    path:
    - 'terraform/*'
    - '.github/workflows/ci-dev.yml'

#
# permissions:
#   contents: read
#   pull-requests: write

jobs:
  terraform:
    uses: "./.github/workflows/terraform_deploy.yaml"
    with:
      env: dev
      branch: dev
